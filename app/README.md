# Moonboard Builder 2016 — App Runtime

Questa cartella contiene il runtime della Shiny app (`app.R`, `ui.R`, `server.R`), i modelli serializzati e le dipendenze Python.

## Avvio rapido

Da root repository:

```r
shiny::runApp("app")
```

Oppure da questa cartella:

```r
shiny::runApp(".")
```
